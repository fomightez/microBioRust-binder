#!/bin/bash

# Build the Rust extension module and install the package into the conda
# environment in development mode. `conda run -n notebook` is needed because
# postBuild runs without the environment activated, so maturin can't find it
# otherwise.
cd microbiorust-py
conda run -n notebook maturin develop